from django.urls import path
from . import views

app_name = 'planetor'
 
urlpatterns = [
    #path('', views.generate_planet, name='planetor'),
    #path('edit_options/', views.edit_options, name='edit_options'),
    #path('generate_planet/', views.generate_planet, name='generate_planet'),
    #path('gallery/', views.gallery, name='gallery'),
]
